{{- if .Values.controller.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator.fullname" . }}-metrics
  labels:
    app.kubernetes.io/role: controller
    {{- include "operator.labels" . | nindent 4 }}
  {{- with .Values.controller.metrics.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.controller.metrics.service.type | default "ClusterIP" }}
  ports:
    {{- $scheme := include "operator.metricsScheme" (dict "secure" .Values.controller.metrics.secure) }}
    {{- $port := include "operator.metricsPort" .Values.controller.metrics }}
    - port: {{ $port }}
      targetPort: {{ $scheme }}
      protocol: TCP
      name: {{ $scheme }}
  selector:
    app.kubernetes.io/role: controller
    {{- include "operator.selectorLabels" . | nindent 4 }}
{{- end }}
